# ──────────────────────────────────────────────
# requirements.txt — Palm Tree Detection API v5
# ──────────────────────────────────────────────

# Web framework & server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # includes httptools & websockets
httptools>=0.6.1

# Async file I/O (dipakai di main.py untuk upload TIF)
aiofiles>=23.2.1

# JSON serialization cepat (ORJSONResponse)
orjson>=3.10.0

# Database PostgreSQL
psycopg2-binary>=2.9.9         # binary build, tidak perlu libpq-dev

# Environment variables
python-dotenv>=1.0.1

# YOLO model inference
ultralytics>=8.2.0

# Geospatial / raster image processing
rasterio>=1.3.10
numpy>=1.26.0
opencv-python-headless>=4.9.0  # cv2, headless (tanpa GUI) cocok untuk server

# Optional: event loop lebih cepat (Linux/Mac only, skip di Windows)
uvloop>=0.19.0; sys_platform != "win32"